---
import Logo from './ui/Logo.astro'

export interface Props {
  lang?: 'ja' | 'en'
}

const { lang = 'ja' } = Astro.props
---

<header class="sticky top-0 z-50 bg-white/70 backdrop-blur-xl border-b border-white/20 shadow-lg shadow-gray-200/20">
  <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo -->
      <Logo />

      <!-- Right side: Language -->
      <div class="flex items-center">
        <!-- Language Dropdown -->
        <div class="relative" id="language-dropdown">
          <button 
            type="button"
            class="inline-flex items-center px-4 py-2.5 rounded-xl text-sm font-semibold text-gray-700 bg-gradient-card hover:bg-white/90 transition-all duration-300 border border-white/30 shadow-lg hover:shadow-xl hover:scale-105 backdrop-blur-sm"
            aria-expanded="false"
            aria-haspopup="true"
            id="language-button"
          >
            <span class="mr-2 text-lg">{lang === 'ja' ? '🇯🇵' : '🇺🇸'}</span>
            <span class="hidden sm:inline">{lang === 'ja' ? '日本語' : 'English'}</span>
            <svg class="ml-2 h-4 w-4 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          
          <div 
            class="absolute right-0 mt-3 w-56 rounded-2xl shadow-2xl bg-white/90 backdrop-blur-xl ring-1 ring-black/5 hidden border border-white/20 overflow-hidden"
            id="language-menu"
            role="menu"
            aria-orientation="vertical"
            aria-labelledby="language-button"
          >
            <div class="py-2" role="none">
              <a 
                href="/" 
                class="flex items-center px-4 py-3 text-sm font-medium text-gray-700 hover:bg-gradient-to-r hover:from-brand-50 hover:to-accent-50 transition-all duration-200 hover:text-brand-700 group"
                role="menuitem"
              >
                <span class="mr-3 text-lg group-hover:scale-110 transition-transform">🇯🇵</span>
                <span>日本語</span>
                <span class="ml-auto text-xs text-gray-400 group-hover:text-brand-500">JA</span>
              </a>
              <a 
                href="/en" 
                class="flex items-center px-4 py-3 text-sm font-medium text-gray-700 hover:bg-gradient-to-r hover:from-brand-50 hover:to-accent-50 transition-all duration-200 hover:text-brand-700 group"
                role="menuitem"
              >
                <span class="mr-3 text-lg group-hover:scale-110 transition-transform">🇺🇸</span>
                <span>English</span>
                <span class="ml-auto text-xs text-gray-400 group-hover:text-brand-500">EN</span>
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </nav>
</header>

<script>
  // Language dropdown functionality
  document.addEventListener('DOMContentLoaded', () => {
    const languageButton = document.getElementById('language-button');
    const languageMenu = document.getElementById('language-menu');
    
    if (languageButton && languageMenu) {
      languageButton.addEventListener('click', () => {
        const isHidden = languageMenu.classList.contains('hidden');
        if (isHidden) {
          languageMenu.classList.remove('hidden');
          languageButton.setAttribute('aria-expanded', 'true');
        } else {
          languageMenu.classList.add('hidden');
          languageButton.setAttribute('aria-expanded', 'false');
        }
      });
      
      // Close dropdown when clicking outside
      document.addEventListener('click', (event) => {
        if (!languageButton.contains(event.target) && !languageMenu.contains(event.target)) {
          languageMenu.classList.add('hidden');
          languageButton.setAttribute('aria-expanded', 'false');
        }
      });
    }
  });
</script>