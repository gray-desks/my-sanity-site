---
export interface Props {
  lang?: 'ja' | 'en'
}

const { lang = 'ja' } = Astro.props

const title = lang === 'ja' 
  ? '日本全国の旅ログを発信' 
  : 'Discover Japan Through Travel Stories'

const subtitle = lang === 'ja'
  ? '美しい景色、美味しい食べ物、文化体験を多言語で共有'
  : 'Beautiful places, delicious food, and cultural experiences shared in multiple languages'

const ctaText = lang === 'ja' ? '記事を読む' : 'Read Stories'
---

<section class="relative overflow-hidden bg-gradient-animated animate-gradient py-32 lg:py-40 min-h-[80vh] flex items-center">
  <!-- Animated Background Layers -->
  <div class="absolute inset-0 bg-gradient-mesh animate-gradient opacity-30"></div>
  <div class="absolute inset-0 bg-gradient-to-br from-primary-500/30 via-accent-500/20 to-sunset-500/30"></div>
  
  <!-- Japan Map SVG Background with Animation -->
  <div class="absolute inset-0 opacity-5 animate-pulse-slow">
    <svg class="w-full h-full object-cover scale-150" viewBox="0 0 400 300" fill="none" xmlns="http://www.w3.org/2000/svg">
      <!-- Enhanced Japan outline with glow effect -->
      <defs>
        <filter id="glow">
          <feGaussianBlur stdDeviation="3" result="coloredBlur"/>
          <feMerge> 
            <feMergeNode in="coloredBlur"/>
            <feMergeNode in="SourceGraphic"/>
          </feMerge>
        </filter>
      </defs>
      <path d="M300 50c20 0 40 15 45 35l15 25c5 15-5 30-20 35l-25 15c-15 5-30 20-25 40l10 30c5 20-10 35-30 40l-40 10c-25 5-45-10-50-35l-5-25c-5-20-25-30-45-25l-30 10c-20 5-40-15-35-35l5-20c5-15-5-35-25-40l-20-10c-15-5-25-25-15-40l15-25c10-15 30-20 45-15l25 10c20 5 40-5 45-25l10-20c5-15 25-25 40-20l25 10c15 5 35 0 40-15l10-15c5-10 20-15 30-10z" 
            fill="currentColor" 
            filter="url(#glow)"
            class="text-white dark:text-gray-200"/>
    </svg>
  </div>
  
  <!-- Floating Geometric Shapes -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-1/4 left-1/4 w-2 h-2 bg-white rounded-full animate-ping opacity-60"></div>
    <div class="absolute top-1/3 right-1/4 w-3 h-3 bg-accent-300 rounded-full animate-bounce opacity-40" style="animation-delay: 1s;"></div>
    <div class="absolute bottom-1/3 left-1/3 w-1 h-1 bg-sunset-300 rounded-full animate-pulse opacity-70" style="animation-delay: 2s;"></div>
    <div class="absolute top-1/2 right-1/3 w-4 h-4 border border-white/30 rounded-full animate-spin opacity-50" style="animation-duration: 8s;"></div>
  </div>
  
  <!-- Content -->
  <div class="relative max-w-6xl mx-auto px-4 text-center">
    <!-- Shimmer effect for title -->
    <div class="relative inline-block mb-6">
      <h1 class="text-5xl md:text-7xl lg:text-8xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-white via-white to-white/80 leading-tight tracking-tight">
        {title}
      </h1>
      <div class="absolute inset-0 text-5xl md:text-7xl lg:text-8xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-primary-200 via-accent-200 to-sunset-200 animate-pulse opacity-50 leading-tight tracking-tight">
        {title}
      </div>
    </div>
    
    <p class="text-xl md:text-2xl text-white/90 mb-12 max-w-4xl mx-auto leading-relaxed font-medium backdrop-blur-sm bg-white/5 rounded-2xl px-6 py-4 border border-white/10">
      {subtitle}
    </p>
    
    <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
      <a 
        href="#articles" 
        class="group relative inline-flex items-center px-10 py-5 bg-white/10 backdrop-blur-md border border-white/20 text-white rounded-2xl font-bold text-lg hover:bg-white hover:text-primary-600 transition-all duration-500 shadow-2xl hover:shadow-white/25 hover:scale-110 overflow-hidden"
      >
        <div class="absolute inset-0 bg-gradient-to-r from-primary-500 to-accent-500 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
        <span class="relative z-10">{ctaText}</span>
        <svg class="relative z-10 w-6 h-6 ml-3 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </a>
      
      <a 
        href={lang === 'ja' ? '/en' : '/'} 
        class="inline-flex items-center px-8 py-4 border-2 border-white/30 text-white/90 rounded-2xl font-semibold backdrop-blur-sm hover:bg-white/10 hover:border-white/50 hover:text-white transition-all duration-300 hover:scale-105"
      >
        {lang === 'ja' ? '🌐 English' : '🌐 日本語'}
      </a>
    </div>
  </div>
  
  <!-- Floating Elements -->
  <div class="absolute top-20 left-10 animate-bounce delay-1000">
    <div class="w-8 h-8 bg-sunset-400 rounded-full opacity-60"></div>
  </div>
  <div class="absolute top-40 right-20 animate-bounce delay-2000">
    <div class="w-6 h-6 bg-accent-400 rounded-full opacity-60"></div>
  </div>
  <div class="absolute bottom-20 left-20 animate-bounce delay-3000">
    <div class="w-4 h-4 bg-primary-300 rounded-full opacity-60"></div>
  </div>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
  }
  
  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
</style>