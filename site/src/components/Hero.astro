---
export interface Props {
  lang?: 'ja' | 'en'
}

const { lang = 'ja' } = Astro.props

const title = lang === 'ja' 
  ? '日本全国の旅ログを発信' 
  : 'Japan Travel Stories'

const subtitle = lang === 'ja'
  ? '美しい景色、美味しい食べ物、文化体験を多言語で共有'
  : 'Beautiful places, delicious food, and cultural experiences shared in multiple languages'

const ctaText = lang === 'ja' ? '記事を読む' : 'Read Stories'
const ctaSecondary = lang === 'ja' ? 'English' : '日本語'
---

<section class="relative overflow-hidden bg-gradient-hero py-24 lg:py-32 min-h-[80vh] flex items-center">
  <!-- Japan Map SVG Background -->
  <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 opacity-5">
    <svg class="w-80 h-60" viewBox="0 0 400 300" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M300 50c20 0 40 15 45 35l15 25c5 15-5 30-20 35l-25 15c-15 5-30 20-25 40l10 30c5 20-10 35-30 40l-40 10c-25 5-45-10-50-35l-5-25c-5-20-25-30-45-25l-30 10c-20 5-40-15-35-35l5-20c5-15-5-35-25-40l-20-10c-15-5-25-25-15-40l15-25c10-15 30-20 45-15l25 10c20 5 40-5 45-25l10-20c5-15 25-25 40-20l25 10c15 5 35 0 40-15l10-15c5-10 20-15 30-10z" 
            fill="currentColor" 
            class="text-white"/>
    </svg>
  </div>
  
  <!-- Radial Highlight Elements -->
  <div class="absolute top-20 left-1/4 w-64 h-64 bg-radial-highlight rounded-full"></div>
  <div class="absolute bottom-32 right-1/3 w-48 h-48 bg-radial-highlight rounded-full"></div>
  <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-radial-highlight rounded-full"></div>
  
  <!-- Content -->
  <div class="relative max-w-6xl mx-auto px-4 text-center">
    <h1 class="text-5xl sm:text-6xl font-black leading-tight drop-shadow-lg text-white mb-6 tracking-tight font-jp">
      {title}
    </h1>
    
    <p class="text-xl md:text-2xl text-white/90 mb-12 max-w-4xl mx-auto leading-relaxed font-medium">
      {subtitle}
    </p>
    
    <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
      <a 
        href="#latest"
        class="inline-flex items-center px-8 py-4 bg-white text-brand-500 rounded-full font-bold text-lg hover:bg-white/95 hover:scale-105 transition-all duration-300 shadow-xl hover:shadow-2xl"
        aria-label={lang === 'ja' ? '最新記事を読む' : 'Read latest articles'}
      >
        {ctaText}
        <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </a>
      
      <a 
        href={lang === 'ja' ? '/en' : '/'} 
        class="inline-flex items-center px-6 py-3 border-2 border-white/50 text-white rounded-full font-semibold hover:bg-white hover:text-brand-500 transition-all duration-300"
        aria-label={lang === 'ja' ? 'Switch to English' : 'Switch to Japanese'}
      >
        🌐 {ctaSecondary}
      </a>
    </div>
  </div>
</section>