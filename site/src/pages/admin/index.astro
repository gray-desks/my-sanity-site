---
export const prerender = false;
import AdminLayout from '../../layouts/AdminLayout.astro';
---

<AdminLayout title="ダッシュボード">
  <!-- Page header -->
  <div class="mb-8">
    <h1 class="text-2xl font-bold text-gray-900">ダッシュボード</h1>
    <p class="mt-1 text-sm text-gray-500">旅ログ管理システムへようこそ</p>
  </div>

  <!-- Stats cards -->
  <div class="hidden md:grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-3 mb-8">
    <!-- Total Articles -->
    <div class="bg-white overflow-hidden shadow-sm rounded-lg border border-gray-200">
      <div class="p-5">
        <div class="flex items-center">
          <div class="flex-shrink-0">
            <div class="w-8 h-8 bg-blue-100 rounded-lg flex items-center justify-center">
              <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
            </div>
          </div>
          <div class="ml-5 w-0 flex-1">
            <dl>
              <dt class="text-sm font-medium text-gray-500 truncate">記事数</dt>
              <dd class="text-lg font-semibold text-gray-900">24</dd>
            </dl>
          </div>
        </div>
      </div>
    </div>

    <!-- Languages -->
    <div class="bg-white overflow-hidden shadow-sm rounded-lg border border-gray-200">
      <div class="p-5">
        <div class="flex items-center">
          <div class="flex-shrink-0">
            <div class="w-8 h-8 bg-purple-100 rounded-lg flex items-center justify-center">
              <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5h12M9 3v2m1.048 9.5A18.022 18.022 0 016.412 9m6.088 9h7M11 21l5-10 5 10M12.751 5C11.783 10.77 8.07 15.61 3 18.129"></path>
              </svg>
            </div>
          </div>
          <div class="ml-5 w-0 flex-1">
            <dl>
              <dt class="text-sm font-medium text-gray-500 truncate">対応言語</dt>
              <dd class="text-lg font-semibold text-gray-900">20</dd>
            </dl>
          </div>
        </div>
      </div>
    </div>

    <!-- Monthly Views -->
    <div class="bg-white overflow-hidden shadow-sm rounded-lg border border-gray-200">
      <div class="p-5">
        <div class="flex items-center">
          <div class="flex-shrink-0">
            <div class="w-8 h-8 bg-orange-100 rounded-lg flex items-center justify-center">
              <svg class="w-5 h-5 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
              </svg>
            </div>
          </div>
          <div class="ml-5 w-0 flex-1">
            <dl>
              <dt class="text-sm font-medium text-gray-500 truncate">月間PV</dt>
              <dd class="text-lg font-semibold text-gray-900">12.4K</dd>
            </dl>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Action cards -->
  <div class="grid grid-cols-1 gap-6 lg:grid-cols-2 mb-8">
    <!-- Article Management -->
    <div class="bg-white shadow-sm rounded-lg border border-gray-200">
      <div class="p-6">
        <div class="flex items-center mb-4">
          <div class="w-10 h-10 bg-purple-100 rounded-lg flex items-center justify-center">
            <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
            </svg>
          </div>
          <div class="ml-4">
            <h3 class="text-lg font-semibold text-gray-900">記事管理</h3>
            <p class="text-sm text-gray-500">ローカル記事の作成・編集・管理</p>
          </div>
        </div>
        
        <div class="space-y-3">
          <button id="new-article-btn" 
                  class="w-full flex justify-center items-center px-4 py-3 border border-transparent text-sm font-medium rounded-lg text-white bg-purple-600 hover:bg-purple-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500 transition-colors">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
            </svg>
            新しい記事を作成
          </button>
          
          <button id="manage-articles-btn" 
                  class="w-full flex justify-center items-center px-4 py-3 border border-gray-300 text-sm font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500 transition-colors">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
            記事一覧を表示
          </button>
          
          <div class="text-xs text-gray-500 flex items-center">
            <div class="w-2 h-2 bg-purple-400 rounded-full mr-2"></div>
            ローカルファイル管理
          </div>
        </div>
      </div>
    </div>

    <!-- Content Management -->
    <div class="bg-white shadow-sm rounded-lg border border-gray-200">
      <div class="p-6">
        <div class="flex items-center mb-4">
          <div class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center">
            <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
            </svg>
          </div>
          <div class="ml-4">
            <h3 class="text-lg font-semibold text-gray-900">コンテンツ管理</h3>
            <p class="text-sm text-gray-500">記事の作成・編集・翻訳管理</p>
          </div>
        </div>
        
        <div class="space-y-3">
          <a href="https://travel-blog-jp.sanity.studio" 
             target="_blank"
             class="w-full flex justify-center items-center px-4 py-3 border border-gray-300 text-sm font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
            </svg>
            Sanity Studio を開く
          </a>
          
          <div class="text-xs text-gray-500 flex items-center">
            <div class="w-2 h-2 bg-green-400 rounded-full mr-2"></div>
            本番環境で利用可能
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Recent Activity -->
  <div class="bg-white shadow-sm rounded-lg border border-gray-200">
    <div class="px-6 py-4 border-b border-gray-200">
      <h3 class="text-lg font-semibold text-gray-900">最近のアクティビティ</h3>
    </div>
    <div class="p-6">
      <div class="space-y-4">
        <div class="flex items-center space-x-3">
          <div class="w-2 h-2 bg-blue-400 rounded-full"></div>
          <div class="flex-1">
            <p class="text-sm text-gray-900">新しい記事「浅草寺の朝の風景」が公開されました</p>
            <p class="text-xs text-gray-500">2時間前</p>
          </div>
        </div>
        <div class="flex items-center space-x-3">
          <div class="w-2 h-2 bg-green-400 rounded-full"></div>
          <div class="flex-1">
            <p class="text-sm text-gray-900">英語・中国語翻訳が完了しました</p>
            <p class="text-xs text-gray-500">5時間前</p>
          </div>
        </div>
        <div class="flex items-center space-x-3">
          <div class="w-2 h-2 bg-yellow-400 rounded-full"></div>
          <div class="flex-1">
            <p class="text-sm text-gray-900">15枚の新しい写真がアップロードされました</p>
            <p class="text-xs text-gray-500">1日前</p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script type="module" is:inline>
    // Check authentication status on page load
    document.addEventListener('DOMContentLoaded', () => {
      const urlParams = new URLSearchParams(window.location.search);
      const statusInfo = document.getElementById('status-info');
      
      if (urlParams.get('auth') === 'success') {
        statusInfo.innerHTML = `
          <div class="w-2 h-2 bg-green-400 rounded-full mr-2"></div>
          Google認証済み
        `;
        statusInfo.className = 'text-xs text-green-600 flex items-center';
        showNotification('Google認証が完了しました', 'success');
        
        // Remove auth parameter from URL
        const newUrl = window.location.pathname;
        window.history.replaceState({}, document.title, newUrl);
      } else if (urlParams.get('error')) {
        const error = urlParams.get('error');
        if (error === 'auth_failed') {
          showNotification('Google認証に失敗しました', 'error');
        } else if (error === 'no_code') {
          showNotification('認証コードが見つかりません', 'error');
        } else if (error === 'server_config') {
          showNotification('サーバー設定エラーです。管理者にお問い合わせください。', 'error');
        }
        
        // Remove error parameter from URL
        const newUrl = window.location.pathname;
        window.history.replaceState({}, document.title, newUrl);
      }
    });

    // Google login button
    document.getElementById('google-login-btn')?.addEventListener('click', () => {
      window.location.href = '/api/auth/google';
    });

    // Download photos button - direct download from specified album
    document.getElementById('dl-btn')?.addEventListener('click', async () => {
      const btn = document.getElementById('dl-btn');
      const originalText = btn.innerHTML;
      
      btn.disabled = true;
      btn.innerHTML = `
        <svg class="animate-spin w-4 h-4 mr-2" fill="none" viewBox="0 0 24 24">
          <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
          <path class="opacity-75" fill="currentColor" d="m4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
        </svg>
        指定アルバムからダウンロード中...
      `;

      try {
        const response = await fetch('/api/photos/download', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ maxPhotos: 20 })
        });

        let result;
        try {
          result = await response.json();
        } catch (jsonError) {
          console.error('Failed to parse JSON response:', jsonError);
          throw new Error(`サーバーエラー (${response.status}): レスポンスの解析に失敗しました`);
        }

        if (!response.ok) {
          // Handle authentication errors specifically
          if (response.status === 401) {
            if (result.code === 'AUTHENTICATION_REQUIRED' || result.code === 'INVALID_TOKENS') {
              showNotification('Google認証が必要です。Googleへ再ログインしてください。', 'warning');
              // Optionally redirect to Google login
              window.location.href = '/api/auth/google';
              return;
            }
          }
          throw new Error(result.error || 'ダウンロードに失敗しました');
        }

        showNotification(`${result.downloaded} 枚の写真をダウンロードしました`, 'success');
      } catch (error) {
        showNotification(error.message, 'error');
      } finally {
        btn.disabled = false;
        btn.innerHTML = originalText;
      }
    });

    // Modal functionality
    document.getElementById('close-modal')?.addEventListener('click', () => {
      document.getElementById('album-modal').classList.add('hidden');
    });

    // Article management
    document.getElementById('new-article-btn')?.addEventListener('click', () => {
      document.getElementById('article-modal').classList.remove('hidden');
    });

    document.getElementById('manage-articles-btn')?.addEventListener('click', () => {
      loadArticles('drafts');
      document.getElementById('articles-list-modal').classList.remove('hidden');
    });

    document.getElementById('close-article-modal')?.addEventListener('click', () => {
      document.getElementById('article-modal').classList.add('hidden');
      clearArticleForm();
    });

    document.getElementById('close-articles-list-modal')?.addEventListener('click', () => {
      document.getElementById('articles-list-modal').classList.add('hidden');
    });

    document.getElementById('cancel-article-btn')?.addEventListener('click', () => {
      document.getElementById('article-modal').classList.add('hidden');
      clearArticleForm();
    });

    // Article form submission
    document.getElementById('article-form')?.addEventListener('submit', async (e) => {
      e.preventDefault();
      await saveArticle();
    });

    // Article filter buttons
    document.getElementById('filter-drafts')?.addEventListener('click', () => {
      loadArticles('drafts');
      updateFilterButtons('drafts');
    });

    document.getElementById('filter-published')?.addEventListener('click', () => {
      loadArticles('published');
      updateFilterButtons('published');
    });




    // Show notification
    function showNotification(message, type = 'info') {
      const colors = {
        success: 'bg-green-50 border-green-200 text-green-800',
        error: 'bg-red-50 border-red-200 text-red-800',
        warning: 'bg-yellow-50 border-yellow-200 text-yellow-800',
        info: 'bg-blue-50 border-blue-200 text-blue-800'
      };

      const icons = {
        success: `<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>`,
        error: `<path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>`,
        warning: `<path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>`,
        info: `<path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>`
      };

      const notification = document.createElement('div');
      notification.className = `fixed top-4 right-4 ${colors[type]} border rounded-lg p-4 shadow-lg z-50 max-w-sm`;
      notification.innerHTML = `
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 20 20">
              ${icons[type]}
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm">${message}</p>
          </div>
        </div>
      `;
      
      document.body.appendChild(notification);
      
      setTimeout(() => {
        notification.remove();
      }, 4000);
    }

    // Article management functions
    async function saveArticle() {
      const title = document.getElementById('article-title').value;
      const type = document.getElementById('article-type').value;
      const body = document.getElementById('article-body').value;
      
      if (!title || !body) {
        showNotification('タイトルと本文は必須です', 'warning');
        return;
      }

      const saveBtn = document.getElementById('save-article-btn');
      const originalText = saveBtn.textContent;
      
      saveBtn.disabled = true;
      saveBtn.textContent = '保存中...';

      try {
        const response = await fetch('/api/content/articles', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ title, type, body })
        });

        const result = await response.json();

        if (!response.ok) {
          throw new Error(result.error || '記事の保存に失敗しました');
        }

        showNotification('記事を下書きとして保存しました', 'success');
        document.getElementById('article-modal').classList.add('hidden');
        clearArticleForm();
      } catch (error) {
        showNotification(error.message, 'error');
      } finally {
        saveBtn.disabled = false;
        saveBtn.textContent = originalText;
      }
    }

    function clearArticleForm() {
      document.getElementById('article-title').value = '';
      document.getElementById('article-type').value = 'spot';
      document.getElementById('article-body').value = '';
    }

    async function loadArticles(type = 'drafts') {
      try {
        const response = await fetch(`/api/content/articles?type=${type}`);
        const data = await response.json();

        if (!response.ok) {
          throw new Error(data.error || '記事の読み込みに失敗しました');
        }

        displayArticles(data.articles, type);
      } catch (error) {
        showNotification(error.message, 'error');
      }
    }

    function displayArticles(articles, type) {
      const articlesList = document.getElementById('articles-list');
      
      if (articles.length === 0) {
        articlesList.innerHTML = `
          <div class="text-center py-8">
            <div class="text-4xl mb-2">📝</div>
            <p class="text-gray-500">${type === 'drafts' ? '下書きがありません' : '公開済み記事がありません'}</p>
          </div>
        `;
        return;
      }

      articlesList.innerHTML = articles.map(article => `
        <div class="border border-gray-200 rounded-lg p-4 mb-3 hover:bg-gray-50">
          <div class="flex items-start justify-between">
            <div class="flex-1">
              <h4 class="text-sm font-semibold text-gray-900 mb-1">${article.title}</h4>
              <div class="flex items-center space-x-3 text-xs text-gray-500 mb-2">
                <span class="inline-flex items-center px-2 py-1 rounded-full bg-gray-100">
                  ${getTypeEmoji(article.type)} ${getTypeLabel(article.type)}
                </span>
                <span>${new Date(article.created).toLocaleDateString('ja-JP')}</span>
                <span class="px-2 py-1 rounded-full ${article.lang === 'ja' ? 'bg-blue-100 text-blue-800' : 'bg-green-100 text-green-800'}">
                  ${article.lang.toUpperCase()}
                </span>
              </div>
              <p class="text-xs text-gray-600 line-clamp-2">${article.bodyPreview}</p>
            </div>
            <div class="ml-4 flex space-x-2">
              <button class="text-blue-600 hover:text-blue-800 text-xs">編集</button>
              <button class="text-red-600 hover:text-red-800 text-xs">削除</button>
            </div>
          </div>
        </div>
      `).join('');
    }

    function updateFilterButtons(activeType) {
      const draftsBtn = document.getElementById('filter-drafts');
      const publishedBtn = document.getElementById('filter-published');
      
      if (activeType === 'drafts') {
        draftsBtn.className = 'px-3 py-1 text-sm rounded-full bg-purple-100 text-purple-800';
        publishedBtn.className = 'px-3 py-1 text-sm rounded-full bg-gray-100 text-gray-600';
      } else {
        draftsBtn.className = 'px-3 py-1 text-sm rounded-full bg-gray-100 text-gray-600';
        publishedBtn.className = 'px-3 py-1 text-sm rounded-full bg-green-100 text-green-800';
      }
    }

    function getTypeEmoji(type) {
      const types = {
        spot: '🗾',
        food: '🍣',
        transport: '🚄',
        hotel: '🛏️',
        note: '📝'
      };
      return types[type] || '📝';
    }

    function getTypeLabel(type) {
      const types = {
        spot: 'スポット',
        food: 'グルメ',
        transport: '交通',
        hotel: '宿泊',
        note: 'メモ'
      };
      return types[type] || 'メモ';
    }



  </script>
</AdminLayout>