---
import Layout from '../../layouts/Layout.astro'
import Gallery from '../../components/Gallery.astro'
import AdSlot from '../../components/AdSlot.astro'
import PortableTextWithAds from '../../components/PortableTextWithAds.astro'
import { getArticleById, client, type Article } from '../../lib/sanity'
import { ADS_SLOTS } from '../../config/ads'
import { OG_IMAGE_VERSION } from '../../lib/env'

const { id } = Astro.params
const article: Article | null = await getArticleById(id!)

if (!article) {
  return Astro.redirect('/404')
}

const formatDate = (dateString: string) => {
  return new Date(dateString).toLocaleDateString('ja-JP', {
    year: 'numeric',
    month: 'long', 
    day: 'numeric'
  })
}

const typeConfig = {
  spot: { emoji: '🗾', label: 'スポット' },
  food: { emoji: '🍣', label: 'グルメ' },
  transport: { emoji: '🚄', label: '交通' },
  hotel: { emoji: '🛏️', label: '宿泊' },
  note: { emoji: '📝', label: 'メモ' }
}

const config = typeConfig[article.type as keyof typeof typeConfig] || typeConfig.note
---

<Layout 
  title={article.title} 
  description={article.bodyText?.slice(0, 160) || `${article.title}の記事です。`}
>
  <!-- Hero Image (Mobile Full-bleed, 16:9 to match cards) -->
  {article.coverImage?.asset?.url && (
    <picture class="block md:hidden -mx-4 sm:-mx-6 mb-4">
      <img 
        src={`${article.coverImage.asset.url}?w=1200&h=675&fit=crop&auto=format`}
        alt={article.title}
        class="w-full aspect-video object-cover rounded-none cursor-pointer"
        id="cover-image-mobile"
        data-lightbox-url={`${article.coverImage.asset.url}?auto=format`}
        loading="eager"
      />
    </picture>
  )}
  <!-- Mobile wrapper with padding -->
  <div class="px-4 md:px-0">
    <div class="max-w-7xl mx-auto md:px-4 py-8">
      <!-- Main content in center column, sidebar on desktop -->
      <div class="lg:grid lg:grid-cols-4 lg:gap-8">
        <!-- Main Content -->
        <div class="lg:col-span-3 bg-white md:rounded-2xl md:shadow-lg">
          <div class="md:p-8">
          <!-- Cover Image (最上部) -->
          {article.coverImage?.asset?.url && (
            <div class="mb-8 md:rounded-2xl overflow-hidden md:shadow-lg hidden md:block">
              <img 
                src={`${article.coverImage.asset.url}?w=1200&h=600&fit=crop&auto=format`}
                alt={article.title}
                class="w-full h-96 object-cover cursor-pointer"
                width="1200"
                height="600"
                decoding="async"
                fetchpriority="high"
                id="cover-image"
                data-lightbox-url={`${article.coverImage.asset.url}?auto=format`}
              />
            </div>
          )}

          <!-- Title -->
          <header class="mb-6">
            <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
              {article.title}
            </h1>
          </header>


          <!-- Tags and Date -->
          <div class="mb-8 pb-6 border-b border-gray-200">
            <div class="flex flex-wrap items-center gap-4">
              <span class="inline-flex items-center gap-1.5 px-3.5 py-1.5 rounded-2xl text-sm font-medium bg-white/90 text-ink ring-1 ring-[var(--theme-accent)]/10 border border-[var(--theme-accent)]/10 backdrop-blur-sm shadow-sm">
                <span class="-ml-0.5 mr-1.5 drop-shadow-[0_1px_0_rgba(0,0,0,0.25)]">{config.emoji}</span>
                {config.label}
              </span>
              {article.placeName && (
                <div class="flex items-center text-gray-600">
                  <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                  </svg>
                  <span>{article.placeName}</span>
                </div>
              )}
              <time datetime={article.publishedAt} class="flex items-center text-gray-600">
                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 002-2H5a2 2 0 002 2z"></path>
                </svg>
                <span class="ml-1">{formatDate(article.publishedAt)}</span>
              </time>
            </div>
            <!-- Share to Instagram Stories button -->
            <div class="mt-6 flex justify-end">
              <button id="share-to-ig" data-article-id={id} data-ogv={OG_IMAGE_VERSION} class="group inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-white/90 text-ink ring-1 ring-[var(--theme-accent)]/15 border border-[var(--theme-accent)]/15 backdrop-blur-sm shadow-sm hover:bg-white hover:ring-[var(--theme-accent)]/25 focus:outline-none focus:ring-2 focus:ring-[var(--theme-accent)]/25 focus:ring-offset-1 focus:ring-offset-white transition-colors duration-200 disabled:opacity-60" aria-label="ストーリーズにシェア">
                <span class="inline-flex items-center justify-center w-6 h-6 rounded-lg bg-gradient-to-br from-[#f9ce34] via-[#ee2a7b] to-[#6228d7] text-white">
                  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" class="w-4 h-4">
                    <path d="M7.5 2.25h9A5.25 5.25 0 0 1 21.75 7.5v9a5.25 5.25 0 0 1-5.25 5.25h-9A5.25 5.25 0 0 1 2.25 16.5v-9A5.25 5.25 0 0 1 7.5 2.25Zm9 1.5h-9A3.75 3.75 0 0 0 3.75 7.5v9A3.75 3.75 0 0 0 7.5 20.25h9A3.75 3.75 0 0 0 20.25 16.5v-9A3.75 3.75 0 0 0 16.5 3.75Zm-1.125 2.25a1.125 1.125 0 1 1 0 2.25 1.125 1.125 0 0 1 0-2.25ZM12 7.5a4.5 4.5 0 1 1 0 9 4.5 4.5 0 0 1 0-9Zm0 1.5a3 3 0 1 0 0 6 3 3 0 0 0 0-6Z"/>
                  </svg>
                </span>
                <span>ストーリーズにシェア</span>
              </button>
            </div>
          </div>

          <!-- Content -->
          <article class="mb-8 mt-8">
            <div class="prose prose-neutral max-w-none px-0">
              {article.body && article.body.length > 0 ? (
                <div class="article-content">
                  <PortableTextWithAds value={article.body} lang="ja" showAds={false} />
                </div>
              ) : (
                <div class="text-gray-800 leading-relaxed text-lg">
                  {article.bodyText || '記事の内容がありません。'}
                </div>
              )}
            </div>
          </article>


          <!-- Gallery -->
          {article.gallery && article.gallery.length > 0 && (
            <Gallery images={article.gallery} />
          )}

          <!-- Navigation -->
          <div class="mt-12 text-center">
            <a 
              href="/" 
              onclick="event.preventDefault(); if (history.length > 1) { history.back(); } else { location.href='/' }" 
              class="inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-white/90 text-ink ring-1 ring-[var(--theme-accent)]/15 border border-[var(--theme-accent)]/15 backdrop-blur-sm shadow-sm hover:bg-white hover:ring-[var(--theme-accent)]/25 transition-colors"
            >
              ← 記事一覧に戻る
            </a>
          </div>
          </div>
        </div>
        
        <!-- Sidebar - Desktop only -->
        <div class="hidden lg:block lg:col-span-1 space-y-6">
        <!-- Sticky Sidebar Ad -->
        <div class="sticky top-8">
          <AdSlot
            slotId={ADS_SLOTS.SIDEBAR_TOP}
            format="rectangle"
            reserveHeight={280}
            label="広告"
            class="bg-white rounded-2xl shadow-lg p-4"
          />
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Cover Image Lightbox Modal -->
  {article.coverImage?.asset?.url && (
    <div id="cover-lightbox" class="lightbox-overlay hidden fixed inset-0 bg-black bg-opacity-90 z-50 flex items-center justify-center p-4">
      <div class="relative max-w-7xl max-h-full flex items-center justify-center">
        <!-- Close button -->
        <button id="cover-lightbox-close" class="absolute top-4 right-4 text-white text-2xl hover:text-gray-300 transition-colors z-10 bg-black bg-opacity-50 rounded-full w-10 h-10 flex items-center justify-center">
          ×
        </button>
        
        <!-- Lightbox main image -->
        <img 
          id="cover-lightbox-image"
          src={`${article.coverImage.asset.url}?auto=format`}
          alt={article.title}
          class="max-w-full max-h-full object-contain cursor-pointer"
        />
      </div>
    </div>
  )}

  <!-- Instagram Story Share Modal (fallback) -->
  <div id="ig-share-modal" class="lightbox-overlay hidden fixed inset-0 bg-black/80 z-50 flex items-center justify-center p-4">
    <div class="relative w-full max-w-md bg-white rounded-2xl shadow-xl overflow-hidden">
      <button id="ig-modal-close" class="absolute top-3 right-3 w-10 h-10 rounded-full bg-black/70 text-white text-xl">×</button>
      <div class="p-5 space-y-4">
        <h2 class="text-lg font-semibold">Instagramストーリーズにシェア</h2>
        <p class="text-sm text-gray-600">画像を保存してInstagramアプリでストーリーズに貼り付けてください。</p>
        <div class="rounded-xl overflow-hidden border border-gray-200">
          <img id="ig-preview" alt="Story Preview" class="w-full h-auto max-h-[60vh] object-contain bg-gray-50 hidden" />
          <div id="ig-loading" class="w-full py-8 flex flex-col items-center justify-center text-gray-600">
            <svg class="animate-spin h-6 w-6 text-gray-400 mb-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" aria-hidden="true">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z"></path>
            </svg>
            <p class="text-sm">画像を生成しています…</p>
          </div>
        </div>
        <div class="flex flex-col sm:flex-row gap-3">
          <button id="ig-copy-link" class="flex-1 inline-flex items-center justify-center px-4 py-2 rounded-full bg-white/90 text-ink ring-1 ring-[var(--theme-accent)]/15 border border-[var(--theme-accent)]/15 backdrop-blur-sm shadow-sm hover:bg-white hover:ring-[var(--theme-accent)]/25 transition-colors">リンクをコピー</button>
          <a id="ig-download" class="flex-1 inline-flex items-center justify-center px-4 py-2 rounded-full bg-[var(--theme-accent)] text-white shadow-md hover:shadow-lg hover:bg-[var(--theme-accent)]/90 focus:outline-none focus:ring-2 focus:ring-[var(--theme-accent)]/60 transition" download="tabi-log-story.webp">画像を保存</a>
        </div>
        <p class="text-xs text-gray-500">保存できない場合は、画像を長押しして保存してからInstagramを開いてください。</p>
      </div>
    </div>
  </div>

</Layout>

<!-- External client script for lightbox and Instagram share -->
<script src="/js/article-share.js" defer></script>

<style is:global>
  /* Improved Content Typography for Mobile */
  .prose {
    @apply leading-relaxed;
  }
  
  .prose p {
    @apply my-4;
  }
  
  .prose h2 {
    @apply text-xl font-bold mt-10 mb-4 pb-2 border-b border-neutral-200;
  }
  
  .prose h3 {
    @apply text-lg font-semibold mt-8 mb-3;
  }
  
  .prose ul {
    @apply pl-5 my-4;
  }
  
  .prose li {
    @apply my-1;
  }
  
  .prose img {
    @apply rounded-lg;
  }
  
  /* Lightbox styles */
  .lightbox-overlay {
    backdrop-filter: blur(4px);
    animation: fadeIn 0.2s ease-out;
  }
  
  .lightbox-overlay.hidden {
    display: none !important;
  }
  
  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
  
  .lightbox-overlay img {
    max-height: 90vh;
    max-width: 90vw;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
  }
  
  .lightbox-overlay button {
    user-select: none;
    outline: none;
  }
  
  .lightbox-overlay button:focus {
    outline: 2px solid white;
    outline-offset: 2px;
  }
</style>