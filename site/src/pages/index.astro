---
import Layout from '../layouts/Layout.astro'
import AdSense from '../components/AdSense.astro'
import { getJapanesePosts, type Post } from '../lib/sanity'

let posts: Post[] = []
try {
  posts = await getJapanesePosts()
} catch (error) {
  console.error('Failed to fetch posts:', error)
}

const formatDate = (dateString: string) => {
  return new Date(dateString).toLocaleDateString('ja-JP', {
    year: 'numeric',
    month: 'long',
    day: 'numeric'
  })
}
---

<Layout 
  title="旅ログ - 日本全国の旅記録" 
  description="日本全国の旅ログを多言語で発信するブログ。各地の魅力を写真と文章でお届けします。"
>
  <div class="space-y-8">
    <!-- ヒーローセクション -->
    <section class="text-center py-12 bg-gradient-to-r from-blue-500 to-green-500 text-white rounded-lg">
      <h1 class="text-4xl md:text-6xl font-bold mb-4">🗾 旅ログ</h1>
      <p class="text-xl md:text-2xl opacity-90">日本全国の旅記録を多言語で発信</p>
      <p class="mt-4 text-lg opacity-80">各地の魅力を写真と文章でお届けします</p>
    </section>

    <!-- 記事一覧 -->
    <section>
      <h2 class="text-3xl font-bold mb-8 text-center">最新の旅記録</h2>
      
      {posts.length === 0 ? (
        <div class="text-center py-12 bg-white rounded-lg shadow">
          <p class="text-gray-500 text-lg">まだ記事がありません。</p>
          <p class="text-gray-400 mt-2">Sanity Studio で最初の記事を作成してください。</p>
        </div>
      ) : (
        <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
          {posts.map((post) => (
            <article class="bg-white rounded-lg shadow hover:shadow-lg transition-shadow">
              {post.mainImage?.asset?.url && (
                <img 
                  src={post.mainImage.asset.url}
                  alt={post.title}
                  class="w-full h-48 object-cover rounded-t-lg"
                />
              )}
              <div class="p-6">
                <h3 class="text-xl font-semibold mb-2 hover:text-blue-600">
                  <a href={`/posts/${post.slug?.current || post._id}`}>
                    {post.title}
                  </a>
                </h3>
                
                {post.excerpt && (
                  <p class="text-gray-600 mb-4 line-clamp-3">{post.excerpt}</p>
                )}
                
                <div class="flex justify-between items-center text-sm text-gray-500">
                  <time datetime={post.publishedAt}>
                    {formatDate(post.publishedAt)}
                  </time>
                  {post.tripDate && (
                    <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded">
                      旅行日: {new Date(post.tripDate).toLocaleDateString('ja-JP')}
                    </span>
                  )}
                </div>
                
                {post.tags && post.tags.length > 0 && (
                  <div class="mt-3 flex flex-wrap gap-1">
                    {post.tags.map(tag => (
                      <span class="bg-gray-100 text-gray-700 px-2 py-1 rounded text-xs">
                        #{tag}
                      </span>
                    ))}
                  </div>
                )}
                
                {(post.travelCost || post.lodgingCost) && (
                  <div class="mt-3 text-xs text-gray-500 border-t pt-2">
                    {post.travelCost && <span>交通費: ¥{post.travelCost.toLocaleString()}</span>}
                    {post.travelCost && post.lodgingCost && <span class="mx-2">|</span>}
                    {post.lodgingCost && <span>宿泊費: ¥{post.lodgingCost.toLocaleString()}</span>}
                  </div>
                )}
              </div>
            </article>
          ))}
        </div>
      )}
    </section>

    <!-- Google AdSense 広告 -->
    <AdSense 
      slot="homepage-bottom"
      format="banner"
      lang="ja"
      className="mt-12"
    />
  </div>
</Layout>

<style>
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
